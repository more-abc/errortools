# Although it is not a submodule, it has its own version.

__version__ = "0.2.13a" 
# NOTE: Naming Convention for version numbers
# Each time a new sub-sub-sub version is built, 
# append a letter to the end of the version number.